<%= form_with model: @user, method: :post, url: users_confirm_path, local: true, id: "user_form" do |f| %>
  <div class="form-group">
    <div class="text-left mb-1">
      <%= f.label :name %>
    </div>
    <div class="mb-3">
      <%= f.text_field :name, value: current_user&.name, class: "form-control", placeholder: "(例)オーシャン太郎", required: true %>
    </div>
  </div>
  <div class="form-group">
    <div class="text-left mb-1">
      <%= f.label :email %>
    </div>
    <div class="mb-3">
      <%= f.email_field :email, value: current_user&.email, class: "form-control", placeholder: "(例)lunchcafe@ocean.com", required: true %>
    </div>
  </div>
  <div class="form-group">
    <div class="text-left mb-1">
      <%= f.label :phonenumber %>
    </div>
    <div class="mb-3">
      <%= f.telephone_field :phonenumber, value: current_user&.phonenumber, class: "form-control", placeholder: "(例)09012345678", required: true %>
    </div>
  </div>
  <div class="form-group">
    <div class="text-left mb-1">
      <%= f.label :password %>
    </div>
    <div class="mb-3">
      <%= f.password_field :password, class: "form-control", placeholder: "8文字以上32字以内の半角英数字", required: true %>
    </div>
  </div>
  <div class="form-group">
    <div class="text-left mb-1">
      <%= f.label :password_confirmation , "パスワード(確認用)"%>
    </div>
    <div class="mb-3">
      <%= f.password_field :password_confirmation, class: "form-control", placeholder: "パスワードを再度入力", required: true %>
    </div>
  </div>
  <div class="text-left">
    <small>・会員登録をして頂くと簡単にご予約ができるようになります。</small><br>
    <small>・全ての入力項目が必須となっております。</small><br>
    <small>・電話番号はハイフンを除き市外局番から記入をお願いいたします。</small><br>
    <small>・英数字は半角での記入をお願いいたします。</small><br>
  </div>
  <div class="actions text-center my-2">
    <%= f.submit "規約に同意して次へ", class: "btn btn-success w-100" %>
  </div>
<% end %>
